# The packages this option installs for itself. Generated by `npm install`,
# never part of the repository. `package.json` and `package-lock.json` beside
# this file are what say which versions to fetch.
node_modules/
