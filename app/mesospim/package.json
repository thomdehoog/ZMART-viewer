{
  "name": "mesospim-view-page",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A native neuroglancer page driven by mesospim_view (Python).",
  "scripts": {
    "postinstall": "node scripts/patch_neuroglancer.mjs",
    "build": "node scripts/patch_neuroglancer.mjs && node scripts/precompile-workers.mjs && vite build && node scripts/copy-async-worker.mjs",
    "dev": "vite"
  },
  "dependencies": {
    "neuroglancer": "2.41.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vite": "^7.0.0"
  }
}
